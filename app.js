const heading = React.createElement("h2",{id:"heading"},"Hello Vardhit!!!"); // First create Element like h1 tag
const root = ReactDOM.createRoot(document.getElementById("root")) // Then using React DOM create root in that getElementbyID that is root
root.render(heading) //Finally we are rendering element that we have created for heading